{
  "name": "projeto1",
  "version": "0.1.0",
  "description": "PROJETO - SERVIDOR PROJETO 1",
  "scripts": {
    "local": "docker-compose -f docker-compose.yml -f docker-compose.local.yml",
    "local:seed": "docker-compose -f docker-compose.yml -f docker-compose.local.yml exec backend npx knex seed:run"
  },
  "author": "Gabriel Belarmino",
  "private": true
}
